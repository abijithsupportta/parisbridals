# Keep Gradle memory in check on a 12 GB Windows machine.
# The previous 8G heap caused paging and made the IDE feel slow during Flutter work.
org.gradle.jvmargs=-Xmx3G -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
